import styled from "styled-components";

export const DashboardMenu = styled.div`
  margin-top: 4rem;
  .dashboard-heading {
    color: rgb(148 163 184);
    text-transform: uppercase;
    font-size: 0.9rem;
    margin: 0.6rem 0;
  }

  .accrodian-dashboard-button {
    border-radius: 0.2rem;
    transition: 0.5s;
    cursor: pointer;

    &:hover {
      background: #333a48;
    }
  }
`;
